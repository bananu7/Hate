machine:
  ghc:
    version: 7.8.3

dependencies:
  pre:
    - cabal update
    - cabal sandbox init
    - cabal install cpphs
  override:
    - cabal install --dependencies-only
    - cabal build
  cache_directories:
    - .cabal-sandbox

